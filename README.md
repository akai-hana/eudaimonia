# HOW TO USE
1. Clone the repository as usual (preferably on $HOME): `git clone https://github.com/akai-hana/eudaimonia`
2. `cd` into it and run the convenience script to pull the contents of all sub-modules: `./pull-submodules.sh`
3. Enjoy :-)
